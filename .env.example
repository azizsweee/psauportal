# ===== Firebase / Firestore (for Cloud Run / production) =====
# Option A: Base64-encoded service account JSON (recommended for Cloud Run)
# Encode your JSON key file: base64 path/to/key.json (Linux/Mac) or certutil -encode (Windows)
FIREBASE_SERVICE_ACCOUNT_BASE64=

# Option B: File path to service account JSON (local dev only)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

# ===== Admin Credentials =====
ADMIN_USER=447051601
ADMIN_PASS=AzozS2005519
ADMIN_EMAIL=abdulazizsowaankau@gmail.com

# ===== Gemini AI API Key =====
GEMINI_API_KEY=AIzaSyDmQrbecOdkcdRL4sfwmhqqk1US869ZnbU

# ===== SMTP Configuration =====
# Option A: Brevo (recommended for production — free tier: 300 emails/day)
# Get SMTP keys from https://app.brevo.com/settings/keys/smtp
# BREVO_HOST=smtp-relay.brevo.com
# BREVO_PORT=587
# BREVO_USER=your_brevo_email@example.com
# BREVO_PASS=your_brevo_smtp_key

# Option B: Generic SMTP (Gmail, Outlook, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# Option C: mail-config.json (auto-generated file in project root, checked into git)
